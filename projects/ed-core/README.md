# ed-core

Engine + plumbing + shared flight primitives + the registry/active-set runtime
for the ED-AFK bot, plus the CLI host that selects and runs the active app set.
Imports DOWN into ed-vision only; never imports a domain.

Part of the ED-AFK workspace. See the repo root for the full project.
