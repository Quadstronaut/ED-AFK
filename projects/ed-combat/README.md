# ed-combat

Combat domain for the ED-AFK bot. Phase 1: an empty scaffold that registers
nothing and runs solo. Real build is Phase 2. Depends on ed-core + ed-vision;
never imports a sibling domain.

Part of the ED-AFK workspace. See the repo root for the full project.
