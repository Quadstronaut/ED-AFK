# ed-vision

Pure perception for the ED-AFK bot — frames in, measurements out. The bottom
leaf of the workspace dependency DAG: it sends no keys, runs no maneuver, and
imports nothing else in the workspace.

Part of the ED-AFK workspace. See the repo root for the full project.
