<!-- C-council C1-cv-action-family | task waqq2s5lt | decision=route_back selected= | 2026-06-17 | DESIGN-ONLY (not built/wired) | ledger: .claude/council-ledger.jsonl -->
<!-- STATUS 2026-06-18 (L5): DESIGN-ONLY / no-build is LIFTED per the MASTER-SPEC standing rules — building is authorized for ratified scenes. The "(not built/wired)" marker above is the historical design-only record, not a current prohibition. NO-GUESSING + fail-closed still bind. -->

DELIVERABLE: a DESIGN-ONLY doc (no code, no commit, no registered-step bodies, no gate flip) at:
<repo-root>\ED-AFK\.claude\worktrees\wf_e93e77aa-776-2\docs\superpowers\specs\2026-06-17-c1-cv-action-family-DESIGN-gen-opus-1.md

=== CORE DESIGN DECISIONS (per AC) ===

THE LOAD-BEARING CONSTRAINT (drives everything): two cited repo facts â€” (a) the READ layer is validated ONLY on the row LIST, never a detail-page button bar (ocr_winrt.py / navpanel_reader.py docstrings, Sol 8/8 body names); (b) the bright-orange SELECTED element's NAME does not OCR cleanly (prior art 2026-06-16-cv-navpanel-target-DESIGN-DRAFT.md + memory ed-navpanel-ocr-first-parser ROUND 2). There is NO detail-page button-bar fixture in the repo (verified by enumerating tests/fixtures/navpanel/ â€” all PNGs are LIST/full views or icon templates; even selection-marker.png is a selected-ROW icon, not a button label). Therefore S0's calibration is BLOCKED-ON-KYLE and S1-S4 ALL fail closed when the S0 read is unavailable â€” NO blind positional fallback (that is the 10:30Z wrong-target bug class). The legacy blind macros stay a separate fallback path, not a silent degrade target.

MODULE PLACEMENT (G12 core->domain honoured): S0 primitive + ButtonKind enum + ButtonBarResult go in ed-vision/navpanel_detail.py (CV substrate, co-located with navpanel_reader.py/ocr_winrt.py); S0 takes sender/sleeper/grabber/ocr_fn as INJECTED callables so it never imports ed-core. S1-S4 registered steps go in ed-core/flow/steps_navpanel_cv.py and call INTO ed-vision's S0 (core->vision is allowed; steps_body_tour already does from ed_vision...). S0 is a primitive, NEVER in STEP_REGISTRY.

S0 navpanel_detail_button_bar (AC1): signature + ButtonKind{LOCK,UNLOCK,ACTIVATE_SC,DEACTIVATE_SC,UNKNOWN} + ButtonBarResult{label_read,kind,target_reached,pressed} specified. Label crop region SOURCE = resolve_nav_region(...) RESULT passed down by the caller (NEVER a constant). The cursor walk RE-READS the label after EVERY UI_Right move and only presses UI_Select once after the read confirms the cursor is on the requested slot â€” press count is READ-CONFIRMED, not positionally assumed. Toggle-aware accept-sets ({LOCK,UNLOCK} for lock slot, {ACTIVATE_SC,DEACTIVATE_SC} for sc-assist slot). Fail closed on OCR-empty/UNKNOWN entry or mid-walk, or max_walk exhausted. Recommended read strategy: whole-bar OCR + position (non-highlighted buttons OCR clean) over highlight-band crop (the case prior art says fails) â€” choice BLOCKED-ON-KYLE B4.

S1 nav_target_star (AC2): input_exclusive=True. Open star row (system-name row) detail page; read lock label. UNLOCK -> already locked -> NO-OP success, no press. LOCK -> press lock ONCE (x2 EXPLICITLY FORBIDDEN â€” legacy did UI_Select x2). Verify _destination_is_local_star(st,system) is True. Fail-closed paths enumerated: row-not-found-by-name, UNKNOWN label, verification not True (status+system wired), focus unrestorable, bind missing. NEVER double-press.

S2 nav_supercruise_star (AC3): input_exclusive=True. Carries in-supercruise guard (refuse if not in SC). Via S0 reach the SUPERCRUISE ASSIST slot (NOT lock), read-before-toggle: DEACTIVATE_SC label -> assist already on -> NO-OP, DO NOT PRESS (toggle hazard); ACTIVATE_SC -> press once; UNKNOWN -> fail closed. No assist-engaged Status flag exists (cited from step_sc_assist_orbit docstring) -> post-state limited to still-in-SC; mid-macro drop = fail closed. Replaces blind sc_assist_orbit.

S3 nav_supercruise_target (AC3): input_exclusive=True. Targets STATION row on the CONTACTS tab (CycleNextPanel x2) â€” grounded in request_docking's fact (station=position 0 on Contacts; orbited body=position 0 on Navigation). Press SUPERCRUISE ASSIST once via S0; same read-before-toggle + no-assist-flag limit as S2.

S4 nav_supercruise_unexplored (AC4): NOT input_exclusive. Loops next UNEXPLORED row -> detail page -> SUPERCRUISE ASSIST via S0, one body/iteration gated on its AutoScan (mirrors step_body_tour). Terminates on the system-glyph (âœ¦) row by ROW SELECTOR position; fallback terminator = first Ly-distance row (because ocr_winrt strips leading glyphs and may erase âœ¦ â€” B5). Self-guards each per-iteration macro and RELEASES the guard during the per-body wait. Consults should_abort every iteration. Loop-level fail-open-to-resume on list-read error (returns True, never False); per-press still fail-closed on an unread button label.

AC5: every contract states StepContext deps, registration name, input_exclusive flag, fail-closed behaviour â€” in register_step/StepFn shape (ctx, *, kw) -> bool. AC6: every crop sourced via resolve_nav_region; no new bare-constant region; Mandalay-single-ship inert state noted. AC7: pure-logic acceptance tests T0a-T9 specified against FakeOcr/FakeSender/SimpleNamespace suppliers (the exact shapes test_nav_panel_bounded.py + test_body_tour.py use), each mapped to an invariant. AC8: BLOCKED-ON-KYLE B1-B6 consolidated. AC9/AC10: design-only discipline + per-claim citations; four legacy steps NOT removed.

VERIFIED AGAINST REPO (not asserted): the four new names are green-field (grep of all register_step sites = NONE FOUND); UI_Left is absent from REQUIRED_ACTIONS (only UI_Right present, binds_validate.py) = B2 confirmed; no detail-page button-bar fixture despite a rich fixture set = B1 confirmed; resolve_nav_region(default, by_ship, ship) signature confirmed.

No acceptance tests were RUN: AC9 forbids final step bodies, so the tests are specified (T0-T9) as the executable acceptance spec with stub bodies to fill post-ratification. This is correct per DESIGN-ONLY discipline.

## ASSUMPTIONS ()
- DELIVERABLE INTERPRETATION: 'concrete executable acceptance tests are specified' (AC7) under DESIGN-ONLY discipline (AC9, which forbids final step bodies) means a test SPECIFICATION (named tests T0-T9, each mapped to an invariant, with the fake shapes pinned) rather than runnable test code. I did not write or run test bodies, because writing them would require the step implementations AC9 forbids. If the council wanted runnable test stubs that import not-yet-existing steps and xfail, that is a different (and self-contradictory under AC9) reading.
- S0 PLACEMENT: I put the S0 primitive in ed-vision (navpanel_detail.py) rather than ed-core, because it is fundamentally a CV read+classify primitive and ed-vision already owns the read layer. The brief said 'a shared location reachable by ed-core steps ... mirror how navpanel.py executor + navpanel_reader.py split' â€” that split has the key-macro in ed-core and the read in ed-vision. S0 is BOTH (read + cursor walk), and I chose to keep it whole in ed-vision with injected sender/sleeper to preserve testability; an alternative reading puts the cursor-walk half in ed-core/executor and only the read in ed-vision.
- READ STRATEGY: I recommended whole-bar OCR + cursor-position inference over highlight-band OCR, extrapolating from the prior-art fact that non-selected rows OCR clean while the selected one does not. This is a design RECOMMENDATION explicitly gated behind B4 (needs a real frame to confirm non-highlighted button labels OCR) â€” not an asserted mechanic.
- S4 TERMINATOR: I assumed the âœ¦-glyph terminator may be erased by ocr_winrt's glyph-stripping and specified the Ly-distance row (navpanel_reader._is_nearby_system_row) as the citable fallback. This assumes the nearby-systems section always begins at the first Ly row, which holds for the distance-sorted interleaved list per memory ed-navpanel-navigation-tab-format but is flagged BLOCKED (B5) for the glyph specifically.
- REGION FOR S0: the button-bar label crop is a DIFFERENT rect than DEFAULT_NAV_REGION=(505,435,410,330) (which is the list body-name column). I assumed a NEW default rect is needed for the button bar, sourced through resolve_nav_region but BLOCKED-ON-KYLE for its value. I did not reuse the list region for the button bar.
- ACCEPT-SET OWNERSHIP: I assumed the cleaner split is S0 reaches+reads the slot and returns the kind, while the CALLER (S1-S3) decides whether to ask S0 to press (no-op-on-DEACTIVATE etc.), keeping the toggle decision next to the verification predicate. The brief's S0 spec says S0 'presses UI_Select once' â€” I reconciled this by having S0 press when told, with the caller's accept-set/press-if controlling whether a press is requested.
- SHIP SUPPLIER FOR REGION: I assumed resolve_nav_region's `ship` arg is fed from ctx.ship_supplier() (the journal Ship token) with by_ship={} (empty, single-ship Mandalay), making the per-ship hook inert today â€” matching navpanel_reader.resolve_nav_region's documented inert behaviour.

## RISKS ()
- @{lens=spec-conformance; note=AC7 says 'concrete executable acceptance tests are specified ... see acceptance_tests'. I delivered a test SPECIFICATION (T0-T9) not runnable code, on the grounds AC9 forbids implementation bodies. If the council reads AC7 as requiring ACTUALLY RUNNABLE tests (even against stubbed/xfail steps), my deliverable is short of that bar. This is the single most likely conformance gap â€” the two ACs are in tension and I picked the design-only resolution.}
- @{lens=spec-conformance; note=S0 module placement (ed-vision vs ed-core) and the read+walk being one unit vs split could be judged as not faithfully mirroring 'how navpanel.py executor + navpanel_reader.py split' (which separates key-macro from read). A strict reader may want the UI_Right/UI_Select walk in ed-core/executor and only the OCR/classify in ed-vision. My single-unit choice trades that mirror for keeping the read-confirm-after-move loop testable in one place.}
- @{lens=boundaries; note=The whole design rests on uncalibrated button-bar geometry (B1/B3/B4). If the live button bar has a layout my read-confirm walk model doesn't anticipate â€” e.g. a vertical bar (UI_Up/UI_Down to move between buttons, not UI_Right), or buttons that require UI_Left (B2) â€” the S0 walk-direction model is wrong and max_walk/button_order need rework. I bounded this with read-confirm + BLOCKED flags, but the cursor-MOVEMENT axis itself is an unverified assumption (I assumed horizontal UI_Right walk from the legacy positional facts).}
- @{lens=failure-recovery; note=S4's split between loop-level fail-open (list read error -> return True, jump resumes) and per-press fail-closed (button UNKNOWN -> skip one body) is subtle. A failure mode I may have under-specified: if the LIST reads fine but EVERY body's button label is UNKNOWN (e.g. button-bar OCR is systematically broken while list OCR works), S4 would loop, skip every body, and exit having toured nothing â€” silently ineffective rather than loud. The design logs per-skip but doesn't escalate a 100%-button-read-failure to a louder signal.}
- @{lens=security; note=Lowest-relevance lens here (no untrusted input / no auth surface â€” it's a local game macro). The nearest analogue: a mis-calibrated crop region (B4) could make S0 OCR an UNRELATED screen region and classify a stray label as ACTIVATE_SC, causing a wrong press. The read-confirm-after-move loop mitigates a wrong WALK but not a wrong INITIAL region; a region that consistently reads a plausible-but-wrong label would defeat the fail-closed guard. Calibration validation (B4) is the only real defense and it's blocked.}
- @{lens=concurrency; note=S4 is intentionally NOT input_exclusive and releases its per-body guard during the wait so the heat watchdog runs. I mirrored step_body_tour's guard-factory pattern (ctx.exclusive_guard() per body). Risk: if the real exclusive_guard semantics differ from body_tour's nullcontext-fallback assumption, or if a re-engage-on-drop happens mid-wait (S4 inherits body_tour's SupercruiseExit re-engage), the guard enter/exit pairing my T4d test asserts could be wrong. I specified the pattern by analogy without a live concurrency trace of the new step.}
