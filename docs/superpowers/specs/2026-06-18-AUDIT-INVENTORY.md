# ED-AFK Repo Inventory - Council-Verified (2026-06-18)

> ⚠️ **PARTLY SUPERSEDED 2026-06-27** (council audit `wf_d6683762-921`). These 2026-06-18 claims are FALSE at master `0570662` — do not trust them:
> - "ALL 7 C-councils designed-only / zero scene .toml landed" — FALSE: `traversal.toml` (C5, `5512a17`) + the C2 orchestrator (`42f33d0`) LANDED.
> - "C5 Traversal RATIFIED but UNBUILT" — FALSE: built.
> - "PHASE2-EXPLORATION-FLAG `_exploration_mode` phantom not yet fixed" — FALSE: fixed in `cc50366` (now reads `_body_tour_enabled`).
> - "UNWIRED-002 navpanel_icons diagnostic-only, no live step imports it" — FALSE: wired live in `dispatch_route_complete` (route-complete dock/park CV, `0570662`).
> - "C4 smack DEVIATION-BLOCKED" — struck: MASTER-SPEC marks the deviation a Claude error; the smack 8-step routine is LOCKED LAW.
> Current authoritative state: memory `resume-state-2026-06-27-audit-decisions`. The rest below is retained as a historical 2026-06-18 snapshot.

> Autonomous council-v2 inventory (run wf_8218f4e3-228): 4 blind read-only generators (branches/worktree/stubs/spec-state) + adversarial verification. Each item carries a re-runnable PROOF reference. Confidence: verified | unverified | disputed.
> **CAVEAT:** the `completeness` review lens died on a stream-idle timeout, so omission-hunting is PARTIAL. The proof-resolution and judgment-claims lenses completed.

## Summary
Reconciled inventory of the ED-AFK repo at master fbd17ae, fusing 4 blind slices with 3 adversarial lenses. Net: the working tree is clean except 2 untracked files (a C4 Stage-0 spec to commit, and a new import-resolution gate test that legitimately FAILS on 74 post-reorg import rots); 88 worktree-wf_* branches plus 5 of 6 origin feature branches are fully-subsumed cruft (only origin/pips-reset-focus-gate carries 1 unmerged commit b047155); all 7 flow-redesign C-councils are DESIGNED-ONLY (zero scene .toml/modules landed), C3+C5 ratified-unbuilt, C4 deviation-blocked; the smack CV path is a permanent NONE-stub making STARSMACK unreachable. Two adversarial corrections were binding and are applied: ed_trading is live-wired as a no-op (NOT dead-delete), and the worktree branch count is 88/largest-group-47 (not 85/44).

## Branches to delete (cruft)
- **88 local worktree-wf_* branches across 9 distinct tips â€” all IS_ANCESTOR of master, all deletable** - Abandoned council-workflow generation seats. 9 tip-SHA groups, all confirmed IS_ANCESTOR of master with empty master..<tip> diffs. CORRECTED COUNT (adversarial judgment lens): total is 88, not the slice's 85. Per-group: tip 1ad0746 = 47 branches (CORRECTED from 44 â€” the slice under-counted wf_bc37f748-3ce-9, -10 and wf_e93e77aa-776-9, all re-verified at 1ad0746/IS_ANCESTOR); bee559b = 10; 96e24fc = 14; 830c837 = 8; d2f1a76 = 2; 1007187 = 2; 4209044 = 4; 5cd73b9 = 1; 7017b76 = 4. Sum = 92 across the per-group lists in the slice, but git branch|grep worktree|wc -l authoritatively returns 88 (the slice's own range notations double-described a few seats); the load-bearing fact â€” every worktree branch is a merged ancestor and safe to delete â€” holds for all of them. Safe bulk delete: git branch | grep worktree | xargs git branch -D.
  - proof: `<repo-root>\ED-AFK :: git branch | grep worktree | wc -l  (=88); git log --format='%H' -1 worktree-wf_bc37f748-3ce-9 worktree-wf_bc37f748-3ce-10 worktree-wf_e93e77aa-776-9  (all =1ad07467â€¦); git merge-base --is-ancestor <each> master && echo IS_ANCESTOR`
  - judgment: **cruft-delete** | confidence: verified
- **origin/dock-approach-fix â€” fully subsumed, 0 unmerged commits** - Tip 5259fbe 'test(dock): fix infinite loop in no_range_state_fallback'. git log master..origin/dock-approach-fix --oneline returns empty. master 150+ commits ahead.
  - proof: `<repo-root>\ED-AFK :: git log master..origin/dock-approach-fix --oneline  (empty)`
  - judgment: **cruft-delete** | confidence: verified
- **origin/fix-audio-test â€” fully subsumed** - Tip b485b8a 'test(audio_wait): hermetic coverage for _default_pycaw_probe'. Empty master..origin diff.
  - proof: `<repo-root>\ED-AFK :: git log master..origin/fix-audio-test --oneline  (empty)`
  - judgment: **cruft-delete** | confidence: verified
- **origin/pips-reset-reliable â€” fully subsumed** - Tip f14444c 'fix(pips): robust ResetPowerDistribution -- 3x with inter-press sleeps'. Empty diff.
  - proof: `<repo-root>\ED-AFK :: git log master..origin/pips-reset-reliable --oneline  (empty)`
  - judgment: **cruft-delete** | confidence: verified
- **origin/sc-proximity-branch â€” fully subsumed** - Tip a070f9b 'fix(flow): 4-priority in-SC launch gate â€” smack guard + fail-safe indeterminate'. Empty diff.
  - proof: `<repo-root>\ED-AFK :: git log master..origin/sc-proximity-branch --oneline  (empty)`
  - judgment: **cruft-delete** | confidence: verified
- **origin/streamline-arrival-navpanel â€” fully subsumed** - Tip 6f931e1 'fix(startup): empty-route guard + target_next_route fast-fail + remove pitch-astern'. Empty diff.
  - proof: `<repo-root>\ED-AFK :: git log master..origin/streamline-arrival-navpanel --oneline  (empty)`
  - judgment: **cruft-delete** | confidence: verified

## Unmerged remote branches to review
- **origin/pips-reset-focus-gate â€” 1 unmerged commit b047155 (cockpit-focus gate on pips steps + 9 tests)** - The ONLY origin feature branch carrying work not in master. b047155 'fix(flow): cockpit-focus gate on reset_power_distribution + pips_engines' adds _ensure_cockpit_focus() at the top of both steps in projects/ed-autojump/src/ed_autojump/flow/steps.py (+38 lines) plus a new projects/ed-autojump/tests/flow/test_reset_power_distribution.py (+135 lines, 9 cases). NOT_ANCESTOR of master; master is ~150 commits ahead of the fork point, so integrating requires cherry-pick or rebase, not fast-forward. Decision owed: cherry-pick the focus gate (it addresses the synthetic-press-timing memory class) or discard. Cross-check against the live reset_power_distribution before cherry-picking â€” master may already carry an equivalent fix from origin/pips-reset-reliable (f14444c, the 3x-with-sleeps variant) that subsumed differently.
  - proof: `<repo-root>\ED-AFK :: git log master..origin/pips-reset-focus-gate --oneline  (=b047155); git merge-base --is-ancestor b047155 master && echo IS_ANCESTOR || echo NOT_ANCESTOR  (=NOT_ANCESTOR)`
  - judgment: **unmerged-review** | confidence: verified

## Working tree: untracked/modified
- **master (HEAD fbd17ae) in sync with origin/master; ZERO modified tracked files** - git status shows no M/A/D lines â€” the only working-tree deltas are the 2 untracked files below. No uncommitted tracked changes to lose.
  - proof: `<repo-root>\ED-AFK :: git status --porcelain=v1 --untracked-files=all  (only two ?? lines); git branch -avv`
  - judgment: **in-sync** | confidence: verified
- **WT-001 docs/superpowers/specs/council-briefs/C4-smack-recovery-STAGE0-SPEC.md â€” UNTRACKED, commit it** - Arbiter-authored Stage-0 design spec for Council C4 (205 lines: 8-step smack-recovery contract, 8 invariants, 10 acceptance criteria, the step-6 engage_jump_clearance-vs-engage_supercruise conflict, full BLOCKED-ON-KYLE list, 8 executable acceptance tests). Design-only; references but does not modify flight code. No committed doc cross-references it yet (produced after fbd17ae). It is the artifact the next C4 council run binds to once the operator reconciles the smack routine deviation. Commit as the authoritative C4 design gate.
  - proof: `<repo-root>\ED-AFK :: git status --porcelain=v1 --untracked-files=all  (?? â€¦/C4-smack-recovery-STAGE0-SPEC.md); git grep -n 'C4-smack-recovery-STAGE0-SPEC' -- .  (empty â€” no committed reference)`
  - judgment: **commit-this** | confidence: verified
- **WT-002 projects/ed-autojump/tests/test_import_resolution.py â€” UNTRACKED; FAILS on 74 violations (legit post-reorg rot, NOT a bad test)** - New pytest gate that walks every workspace src/ and scripts/ tree, extracts absolute imports of workspace packages, and verifies each resolves at runtime â€” catching moved/deleted symbols whole_tree_import_check.py is blind to (per the 2026-06-16 reorg memory). RE-RAN: exactly 74 unresolved workspace imports, test FAILS. Failures are real rot: (a) stale scripts/ imports (e.g. from ed_autojump.config import load_config; from ed_autojump.vision.capture import GdiGrabber; from ed_autojump.launcher.focus import focus_ed_window) and (b) ONE flight-code line: projects/ed-autojump/src/ed_autojump/flow/steps.py:1471 'from ed_vision.hud_sc_indicators import detect_orbiting'. SHARPENED (re-verified beyond the slice): that import target genuinely does not exist (ModuleNotFoundError; no hud_sc_indicators file in ed-vision), BUT it is DEFENSIVELY DEAD at runtime â€” it sits inside step_confirm_orbiting's try/except, and the function returns at line 1464 when hud_grabber is None, which it always is live (lines 1465-1468 comment says so explicitly: a separate ed-vision matcher council must land first). So the test correctly surfaces debt but this is guarded dead code, NOT a live crash. Commit the test WITH a message noting 'FAILING â€” surfaces 74 post-reorg import rots, incl. the unbuilt hud_sc_indicators D3 dependency at steps.py:1471'.
  - proof: `<repo-root>\ED-AFK :: .venv/Scripts/python -m pytest projects/ed-autojump/tests/test_import_resolution.py -q  (FAILED, '74 unresolved workspace import(s)'); python -c 'from ed_vision.hud_sc_indicators import detect_orbiting'  (ModuleNotFoundError); Read steps.py:1460-1481 (None-guard return at 1464 + try/except wrap)`
  - judgment: **commit-this** | confidence: verified
- **Gitignored artifacts (WT-003..WT-008) â€” disposable, NOT repo artifacts** - Per .gitignore: .claude/worktrees/ (~20+ council worktree checkouts, hundreds of MB â€” check for uncommitted work before bulk-deleting, but untracked); projects/ed-autojump/.venv/ (regenerable); .pytest_cache/ at root + ed-autojump (regenerable); compass_zoom.png (CV debug capture at root, regenerable â€” NOT a fixture); projects/ed-autojump/.env (per-machine config); .claude/ settings/lock files (settings.local.json, close-council.ps1, statusline-councils.ps1, scheduled_tasks.lock). No logs/captures/calibration/samples/*.pkl appeared in the ignored listing.
  - proof: `<repo-root>\ED-AFK\.gitignore :: git status --porcelain=v1 --ignored --untracked-files=all | grep '^!!'`
  - judgment: **gitignored-artifact** | confidence: verified

## Stubs & dead code
- **DEAD-002 ed_trading â€” RECLASSIFIED: live-wired no-op scaffold, NOT dead-delete** - BINDING ADVERSARIAL CORRECTION (judgment lens, major). The slice classified ed_trading as 'never imported by any live production path / dead-delete' â€” FACTUALLY WRONG. projects/ed-trading/pyproject.toml:30 declares trading = "ed_trading:activate" under [project.entry-points."ed_autojump.plugins"], and cli.py:493-495 does _ep.load()() for every entry in that group at startup. So ed_trading IS auto-activated on every live run whenever installed. The runtime effect is benign (activate() is a Phase-1 no-op: idempotency-guarded, registers nothing), but deleting the package would remove the reserved plugin slot. Correct classification: empty-scaffold / live-wired-as-no-op. Keep it; do not delete.
  - proof: `<repo-root>\ED-AFK\projects\ed-trading\pyproject.toml:30 :: Read pyproject.toml (line 24 [project.entry-points."ed_autojump.plugins"], line 30 trading = "ed_trading:activate"); Read cli.py:493-495 (for _ep in _meta.entry_points(group="ed_autojump.plugins"): _ep.load()())`
  - judgment: **keep-live-noop** | confidence: verified
- **DEAD-001 ed_combat â€” genuine dead scaffold (no entry-point), safe to leave or delete** - Distinct from ed_trading: ed_combat declares NO [project.entry-points] block at all (re-verified: zero entry-point matches in projects/ed-combat). __init__.py is only __version__ + an activate() returning None. The docstring says it runs SOLO and is a Phase-1 empty scaffold. Never imported anywhere in src. So the slice's 'registers nothing, not in any live path' judgment HOLDS for ed_combat. It is inert whether kept or deleted (not auto-activated). Keep as a Phase-2 placeholder unless the operator wants the tree pruned.
  - proof: `<repo-root>\ED-AFK\projects\ed-combat\pyproject.toml :: Grep 'entry-points|entry_points' in projects/ed-combat -> No matches; grep -rn 'import ed_combat|from ed_combat' projects --include='*.py' -> none in src`
  - judgment: **dead-keep-or-delete** | confidence: verified
- **STUB-001 detect_escape_vector â€” returns NONE unconditionally (calibration-blocked) â†’ STARSMACK unreachable** - RE-VERIFIED: escape_vector.py:92 'return NONE' is the entire body; lines 69-81 document it as a fail-closed stub awaiting 3 operator calibration frames (blue star-smack, purple planet-smack, deliberate-drop negative). Because _route_sc_exit reads NONE as 'deliberate drop, no recovery', the live smack-recovery CV path can NEVER fire. The untracked C4 Stage-0 spec (WT-001) is blocked on this.
  - proof: `<repo-root>\ED-AFK\projects\ed-vision\src\ed_vision\escape_vector.py:62 :: Read escape_vector.py:62-92 (detect_escape_vector body is 'return NONE')`
  - judgment: **stub-to-implement** | confidence: verified
- **STUB-002 escape_vector.region_rect â€” full-frame placeholder (0,0,h*16/9,h)** - RE-VERIFIED at line 55: 'return (0, 0, frame_height * 16 // 9, frame_height)' â€” a full-frame placeholder that never crops to the actual glyph region. Blocked on the operator measuring the glyph bbox at 1080p and encoding REF_HEIGHT-relative constants mirroring station_menu.py.
  - proof: `<repo-root>\ED-AFK\projects\ed-vision\src\ed_vision\escape_vector.py:55 :: Read escape_vector.py:55`
  - judgment: **stub-to-implement** | confidence: verified
- **STUB-003 explore_kind.classify_kind â€” returns KIND_ORBIT unconditionally (STUB-1)** - No per-row KIND source known. Returns KIND_ORBIT for every NavBody, so the S4 DROP branch (stations/POI/carriers) is dead in live flight â€” those targets orbit-timeout into exclusion set E instead of routing to S6 drop-recover. Signature + call sites wired.
  - proof: `<repo-root>\ED-AFK\projects\ed-explore\src\ed_explore\explore_kind.py:41 :: Grep 'KIND|Q6|TODO' explore_kind.py -> line 41 'return KIND_ORBIT  # STUB-1: default ORBIT-conservative'`
  - judgment: **stub-to-implement** | confidence: verified
- **STUB-004 explore_kind.drop_visited â€” returns False unconditionally (STUB-2)** - Fail-closed not-visited sentinel. S4 DROP branch calls it to correlate a station drop to the target name. Always False until the operator identifies the journal event (SupercruiseDestinationDrop / Docked / ApproachSettlement) and implements name correlation. Only reachable once STUB-1 is also filled.
  - proof: `<repo-root>\ED-AFK\projects\ed-explore\src\ed_explore\explore_kind.py :: grep -n 'STUB-2|return False' explore_kind.py`
  - judgment: **stub-to-implement** | confidence: verified
- **STUB-005 explore_filters.filter_screen_focused â€” returns False (STUB-3, GuiFocus int unknown)** - FILTER_SCREEN_GUI_FOCUS = None so always returns False. The S0 filter gate in step_explore fails closed until the operator confirms the GuiFocus integer for the SET FILTERS sub-screen in-game.
  - proof: `<repo-root>\ED-AFK\projects\ed-explore\src\ed_explore\explore_filters.py :: grep -n 'FILTER_SCREEN_GUI_FOCUS|STUB-3|return False' explore_filters.py`
  - judgment: **stub-to-implement** | confidence: verified
- **STUB-006 explore_filters.read_checkbox_states â€” returns None (calibration-pending)** - Returns None until a navpanel_filters_screen.png fixture with known checkbox states is provided and per-row CV accuracy measured. establish_filters short-circuits on this, so S0 never toggles a filter.
  - proof: `<repo-root>\ED-AFK\projects\ed-explore\src\ed_explore\explore_filters.py :: grep -n 'CALIBRATION-PENDING|return None' explore_filters.py`
  - judgment: **stub-to-implement** | confidence: verified
- **STUB-007 explore_filters.establish_filters â€” returns False unconditionally (both stubs unfilled)** - Full read-before-write + toggle + read-after-write impl is deferred with a TODO. Does no toggling: gates on filter_screen_focused (always False) and read_checkbox_states (always None), returns False on both paths.
  - proof: `<repo-root>\ED-AFK\projects\ed-explore\src\ed_explore\explore_filters.py :: grep -n 'TODO.*read-before-write|return False' explore_filters.py`
  - judgment: **stub-to-implement** | confidence: verified
- **STUB-008 explore_kind.classify_kind TODO â€” operator-blocked on Q6 kind source (PROOF ARM CORRECTED)** - Stub substance confirmed correct. PROOF CORRECTION (proof-resolution lens, minor): the slice's grep arm 'TODO.*operator.*kind' matches ZERO lines â€” the actual text at explore_kind.py:35 is 'TODO(operator): how to read body KIND per nav-panel row?' (lowercase 'kind' after a colon, uppercase 'KIND' elsewhere). Use the Q6 arm, which fires lines 37/45/49/54. The body KIND source (planet/star row vs station/POI row in a nav-panel row) is unverified; until filled the DROP/S6 branch is permanently dead.
  - proof: `<repo-root>\ED-AFK\projects\ed-explore\src\ed_explore\explore_kind.py:35 :: Grep 'TODO\(operator\)|Q6|KIND' explore_kind.py -> line 35 TODO text + lines 37/45/49/54 Q6 (the 'TODO.*operator.*kind' regex returns 0)`
  - judgment: **stub-to-implement** | confidence: verified
- **UNWIRED-001 _escape_vector_grabber â€” always None (PROOF CORRECTED to dispatcher.py:200 only)** - FlowRunner.__init__ sets self._escape_vector_grabber = None (dispatcher.py:200). No build_escape_vector_grabber factory exists in capture.py and cli.py never builds one. boot_routes._route_sc_exit reads getattr(runner,'_escape_vector_grabber',None) and abstains if None â†’ smack determination is PERMANENTLY abstained live; even if wired, detect_escape_vector returns NONE (STUB-001). PROOF CORRECTION (both lenses, minor): the only assignment is dispatcher.py:200; the slice's cited grep also listed cli.py, which yields zero matches (correctly â€” it confirms ABSENCE). Cite dispatcher.py:200 as the proof; cli.py-empty is the corroborating absence.
  - proof: `<repo-root>\ED-AFK\projects\ed-core\src\ed_core\flow\dispatcher.py:200 :: grep -n '_escape_vector_grabber' dispatcher.py -> 'self._escape_vector_grabber = None'; same grep on cli.py -> no output (absence confirmed)`
  - judgment: **wired-but-todo** | confidence: verified
- **UNWIRED-002 navpanel_icons module â€” diagnostic-only, no live step imports it** - navpanel_icons.py (scan_navpanel_rows/classify_icon/detect_row_icon) is imported only by cv_debug_cli.py (offline overlay) and its tests. No production step imports it; docstring says it is OFF until wired and the wiring is operator-steered. Pure perception primitive awaiting a live caller.
  - proof: `<repo-root>\ED-AFK\projects\ed-vision\src\ed_vision\navpanel_icons.py :: grep -rn 'navpanel_icons|classify_icon|detect_row_icon|scan_navpanel_rows' projects --include='*.py' | grep -v 'cv_debug_cli|test_|navpanel_icons.py'  (no live hits)`
  - judgment: **wired-but-todo** | confidence: verified
- **UNWIRED-003 / TODO-004 / TODO-005 resolve_nav_region ship=None â€” per-ship nav region dead (issue #19)** - build_navpanel_vision in capture.py calls resolve_nav_region(ship=None) with TODO(#19) because no runtime active-ship supplier is wired. The by_ship lookup path exists but is dead â€” always returns default_region (Mandalay). config.ExplorationConfig.nav_panel_region_by_ship defaults to {} and stays empty. Two fixes both required: populate the dict AND wire the ship supplier. Silently gives wrong CV regions after a ship swap (the per-ship-regions memory, gap #19).
  - proof: `<repo-root>\ED-AFK\projects\ed-vision\src\ed_vision\capture.py :: grep -n 'TODO.*#19|ship=None' capture.py; grep -n 'nav_panel_region_by_ship' projects/ed-core/src/ed_core/config.py`
  - judgment: **wired-but-todo** | confidence: verified
- **UNWIRED-004 C-series EXPLORATION state â€” routes to legacy fallback, no standalone proc** - _STATE_TO_PROC maps CSeriesState.EXPLORATION to ('fallback', None) so classify_startup delegates to _classify_startup_legacy. SceneTemplate proc note: 'honk/FSS/body-tour has NO standalone live procedure yet (Council C bucket B/C)'. The explore step is wired into arrival.toml but not into an EXPLORATION-state procedure.
  - proof: `<repo-root>\ED-AFK\projects\ed-autojump\src\ed_autojump\flow\boot_routes.py :: grep -n 'EXPLORATION' boot_routes.py -> EXPLORATION -> ('fallback', None)`
  - judgment: **wired-but-todo** | confidence: verified
- **UNWIRED-005 C-series RESUME state â€” routes to legacy fallback, no standalone proc** - _STATE_TO_PROC maps CSeriesState.RESUME to ('fallback', None); SceneTemplate.proc=None, comment 're-derive has NO standalone live procedure'. RESUME can be determined (LP4 divergence) but always falls through to legacy classification.
  - proof: `<repo-root>\ED-AFK\projects\ed-autojump\src\ed_autojump\flow\boot_routes.py :: grep -n 'RESUME.*fallback' boot_routes.py`
  - judgment: **wired-but-todo** | confidence: verified
- **TODO-001/002/003 explore step bare-pass bodies â€” intentional no-ops, NOT missing impl** - Three deliberate pass bodies, correctly labelled by the slice: steps_explore._s2_pin_target (focus pre-check; cursor walk deferred to S3), steps_strand_recovery (bare pass after re-engage-fail to honor the NEVER-RAISE/return-True contract), steps_explore._s5_record (structural placeholder; actual record runs inline in the main loop, _s5_record is uncalled). None is a stub-to-fill; flagged for awareness only. _s5_record is technically uncalled dead-but-harmless scaffolding.
  - proof: `<repo-root>\ED-AFK\projects\ed-explore\src\ed_explore\steps_explore.py :: grep -n 'pass|_s5_record|actual logic lives' steps_explore.py; grep -n 'pass|hand the scene' steps_strand_recovery.py`
  - judgment: **intentional-noop** | confidence: verified

## Spec/council state: built vs designed-only
- **ALL 7 flow-redesign C-councils are DESIGNED-ONLY â€” zero scene .toml or modules landed** - Cross-cutting headline. New scene files (traversal.toml, exploration.toml) and new modules (navpanel_detail.py, steps_navpanel_cv.py, navpanel_icon.py-singular, steps_exploration.py) do NOT exist. New steps (4 C1 nav_* steps; step_boost; parse_station_distance_km; request_docking_tail; dest_is_system/dest_is_station/exploration_active; transition_to/_SECTION_TO_PROC/run_arrival_then_branch) are all green-field. The 2 live procedures the redesign rewrites (arrival.toml, smack_recovery.toml) are still their pre-redesign 2026-06-07/08 bodies. Per the consolidated doc build-order (lines 101-104), nothing past C5 builds until the detail-page frame unblocks C1.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-CONSOLIDATED-BLOCKERS.md:101 :: ls of all 6 new files -> 'No such file'; grep -c C2 orchestrator/discriminators -> 0; arrival.toml + smack_recovery.toml still pre-redesign`
  - judgment: **designed-only** | confidence: verified
- **C3 Arrival â€” RATIFIED (commit) but UNBUILT** - One of only two ratified-by-commit section councils. DESIGN doc header: 'RATIFIED â€¦ decision=COMMIT, gen-sonnet-2 (unanimous 4-lens) â€¦ DESIGN-ONLY: not yet built/wired.' Live arrival.toml still the OLD flow: retains every step C3 marks REMOVED (nav_panel_target at 42 + 72, sc_assist_orbit 81, wait s=13.0 82, explore/station_strand_recovery 89-90, refuel_below=0.70 at 52 not the redesign's 0.50) and still ends by jumping (target_next_routeâ†’orientâ†’engage_jump_clearance, 91-95) which the redesign moves to Traversal.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-C3-arrival-DESIGN.md:1 :: Read arrival.toml -> line 42 nav_panel_target, 52 refuel_below=0.70, 81 sc_assist_orbit, 82 wait s=13.0 all present`
  - judgment: **ratified-unbuilt** | confidence: verified
- **C5 Traversal â€” RATIFIED (commit, round-2, gen-sonnet-2) but UNBUILT â€” first frame-independent scene that can ship** - Consolidated doc line 94: 'RATIFIED (round 2, commit, gen-sonnet-2). Frame-independent and buildable now.' Round-1 route_back'd on a fact-error (mis-attributed retry_from='target_next_route' to arrival.toml, falsely cited sc_resume.toml as proven engage_jump_clearance tail); C5 brief lines 38-52 pin the fixes; ledger shows the round-2 commit unanimous. No traversal.toml exists; engage_jump_clearance step is pre-existing (arrival/dock_resume) but the standalone Traversal proc is not created. THIS is the recommended next build.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-C5-traversal-DESIGN.md:1 :: ls procedures/traversal.toml -> 'No such file'; tail .claude/council-ledger.jsonl -> C5 'commit gen-sonnet-2 â€¦ unanimous â€¦ zero blockers'`
  - judgment: **ratified-unbuilt** | confidence: verified
- **C1 CV-action-family â€” route_back (DESIGNED-ONLY), the keystone-blocked council** - DESIGN header: 'decision=route_back â€¦ DESIGN-ONLY'. Proposes new modules navpanel_detail.py + steps_navpanel_cv.py and 4 steps (nav_target_star / nav_supercruise_star / nav_supercruise_target / nav_supercruise_unexplored). None registered, neither module exists. Routed back on the missing detail-page button-bar frame (B1) â€” item A.1, the keystone the whole CV redesign sits on; C3/C4/C6/C7 all build on it.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-C1-cv-action-family-DESIGN.md:1 :: grep register_step 4 names -> none; ls navpanel_detail.py steps_navpanel_cv.py -> both absent`
  - judgment: **designed-route_back** | confidence: verified
- **C2 control-flow â€” route_back (DESIGNED-ONLY); D4 PINNED = Python orchestrator** - DESIGN header: 'decision=route_back â€¦ DESIGN-ONLY'. D4 decision = Python orchestrator in ed_autojump (NOT a TOML goto), operator round-2 pinned (C2 brief 5-18) + mandatory abort-recheck. Discriminators written + 29-test-GREEN but ONLY as a reference impl inside the test file, NOT placed in predicates.py/boot_routes.py. transition_to/_SECTION_TO_PROC/run_arrival_then_branch absent from boot_routes.py (grep 0); dest_is_system/dest_is_station/exploration_active absent from predicates.py (grep 0). predicates.py exists but only holds Phase-1 helpers _destination_is_local_star + _dest_is_named_station.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-C2-control-flow-DESIGN.md:9 :: grep -cE 'transition_to|_SECTION_TO_PROC|run_arrival_then_branch' boot_routes.py -> 0; grep -cE 'dest_is_system|dest_is_station|exploration_active' predicates.py -> 0`
  - judgment: **designed-route_back** | confidence: verified
- **C4 Smack-recovery â€” route_back AND DEVIATION-BLOCKED (hardest-blocked council)** - DESIGN header: 'decision=route_back, gen-sonnet-3 â€¦ DESIGN-ONLY'. Committed brief C4-smack-recovery.md lines 3-10 carry a DO-NOT-RE-FIRE banner: the operator's fresh verbal routine (flip â†’ target NOTHING â†’ engage supercruise â†’ compass-orient â†’ fine-align ESCAPE VECTOR â†’ wait in-SC â†’ nav_supercruise_star) CONFLICTS with the authored master-spec scope (nav_target_star + pitch_compass + target_ahead + engage_jump_clearance) AND with live memory (compass CV is blind to the real escape vector; compass needs a locked target but the verbal routine targets nothing). Consolidated 51-57: must be reconciled by the operator BEFORE build. Step-6 verb settled to engage_supercruise (Key_J) but moot until reconciled. Live smack_recovery.toml is still v7 (line 49 nav_panel_target, 67 engage_supercruise, 79 engage_jump). Plus the untracked Stage-0 spec (WT-001).
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\council-briefs\C4-smack-recovery.md:3 :: Read C4 brief 3-10 + CONSOLIDATED-BLOCKERS.md 51-57; Read smack_recovery.toml -> still v7`
  - judgment: **blocked-on-operator** | confidence: verified
- **C6 Exploration â€” route_back (DESIGNED-ONLY)** - DESIGN header: 'decision=route_back â€¦ DESIGN-ONLY'. Candidate created (in cleaned worktree) navpanel_icon.py (singular; three-way classifier whose CV extractor raises NotImplementedError behind B2) + steps_exploration.py + tests. On the live tree neither exists and no exploration.toml exists. The PRE-EXISTING navpanel_icons.py (plural) is a ratified TWO-way STAR/NON_STAR classifier C6 cites as a partial B1 unblock; the three-way box-in-box/disc split needs real UNEXPLORED frames. Ledger: route_back to Stage 1 on a gen-sonnet-2 security blocker (auto-registration entering live flight) + unresolved B1/B2 CV calibration (real-frame test XFAIL).
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-C6-exploration-DESIGN.md:1 :: ls navpanel_icon.py + steps_exploration.py -> absent; Glob navpanel_icon*.py -> only plural navpanel_icons.py; ls exploration.toml -> absent`
  - judgment: **designed-route_back** | confidence: verified
- **C7 Docking â€” route_back (DESIGNED-ONLY)** - DESIGN header: 'decision=route_back â€¦ DESIGN-ONLY'. Operator round-2 pinned (C7 brief 5-17): OCR-gated request-docking, NFZ != docking-ready, station-name = Status.Destination.Name. Proposes new step_boost, step_dock_close_to_range, request_docking_tail wrapper, and a new parse_station_distance_km parser (READ layer parses only Ls/Ly today). None exist; only pre-existing step_dock_request (steps.py:936). dock.toml exists but is the OLD proc. Ledger: route_back to Stage 1 on missing DockingGranted/DockingDenied outcome gate, abort-at-50%-throttle ram hazard, ReDoS in the proposed distance regex, NFZ-gate-vs-OCR regression risk. Item A.2 = the contacts-tab km-distance frame this leg needs (none exists).
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-C7-docking-DESIGN.md:1 :: grep -rnE 'parse_station_distance_km|request_docking_tail|step_dock_close_to_range|step_boost' projects -> none (only step_dock_request@steps.py:936); ls dock.toml -> exists (old)`
  - judgment: **designed-route_back** | confidence: verified
- **PHASE2-CSERIES 11-state boot determination â€” LANDED LIVE (earlier Phase-2, not redesign)** - BUILT and wired, not design-only. ed_core/boot/scenes.py defines the 11-state CSeriesState enum, DetermineContext (OQ7-resolved smack_kind field), 11 priority-ordered SceneTemplates incl. _det_starsmack (abstains None on bare telemetry), scene_for(), asserts len==11. boot_routes.py wires it: imports scene_for+CSeriesState (17), build_determine_context (61), classifier calls scene_for+_STATE_TO_PROC (286-290), live CV gate _route_sc_exit sets runner._smack_kind (605) and is registered as a SupercruiseExit event-route at priority 100 (686). Matches memory '947b8c5 LANDED live + ship-safe'.
  - proof: `<repo-root>\ED-AFK\projects\ed-core\src\ed_core\boot\scenes.py:254 :: Read scenes.py C_SERIES_SCENES 254-338 (assert len==11 at 336); grep boot_routes.py -> 17/287/605/686`
  - judgment: **ratified-built** | confidence: verified
- **PHASE2-EXPLORATION-FLAG body_tour_enabled wired; runner._exploration_mode is a PHANTOM** - body_tour_enabled is wired config.py:78 -> dispatcher.py:122/157/462 -> context.py:148 (arrival.toml:83 consumes it via the explore step). BUT boot_routes.py:84 builds the C-series exploration_mode field from getattr(runner,'_exploration_mode',False), and '_exploration_mode' is ONLY read there, never assigned anywhere â€” so the C-series EXPLORATION branch is unreachable today (always False). The C2-required FIX (predicate must read ctx.body_tour_enabled, not the phantom) is NOT yet applied.
  - proof: `<repo-root>\ED-AFK\projects\ed-autojump\src\ed_autojump\flow\boot_routes.py:84 :: grep -n '_exploration_mode' boot_routes.py -> only line 84 (read, default False), no assignment; grep body_tour_enabled ed-core -> config.py:78, dispatcher.py:122/157/462, context.py:148`
  - judgment: **blocked-on-operator** | confidence: verified
- **PHASE2-ACT-RETIRE â€” NOT landed (follow-on)** - No act() method or act-retirement marker anywhere in ed_core/flow (grep 'def act('/'.act('/'retire.*act' -> none). The classifier/event-route surfaces are the live routing the C2 orchestrator plugs into; the legacy act()-retire migration is deferred behind the C-series determination wiring. Design/follow-on only.
  - proof: `<repo-root>\ED-AFK\projects\ed-core\src\ed_core\flow :: grep -rnE 'def act\(|\.act\(|retire.*act' ed-core/src/ed_core/flow -> No matches`
  - judgment: **blocked-on-operator** | confidence: verified
- **PHASE2-GATE-SPLIT target_next_route danger-gate 'hole' (steps.py:94) â€” NOT a live bug, no action owed** - steps.py:94 'if ctx.event_waiter is None: return True' fires ONLY on the unwired/unit-test path. Live, event_waiter IS wired, so the D*/N/H/W danger-class refusal at steps.py:112-114 (is_dangerous) runs and fails closed. Pre-existing documented step behavior (the gate-split the C5 round-2 fix clarifies), not a redesign artifact and not a defect.
  - proof: `<repo-root>\ED-AFK\projects\ed-autojump\src\ed_autojump\flow\steps.py:94 :: Read steps.py:86-116 (line 94 None-guard, line 112 is_dangerous refusal); CONSOLIDATED-BLOCKERS.md:97`
  - judgment: **ratified-built** | confidence: verified
- **DOC-COMMIT-STATE â€” all redesign docs committed on master (fbd17ae); only the C4 Stage-0 spec untracked** - The 7 per-council DESIGN docs, CONSOLIDATED-BLOCKERS, MASTER-SPEC, and all 7 committed council-briefs (C1-C7) are git-tracked on master, last touched by fbd17ae. The council ledger (.claude/council-ledger.jsonl) records all 7 runs with decisions (C3 commit, C5 commit, C1/C2/C4/C6/C7 route_back) + the defect/dissent trail. UPDATE 2026-06-21: C4-smack-recovery-STAGE0-SPEC.md is now COMMITTED (f3f5b52); WT-001 cleared.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs :: git ls-files docs/superpowers/specs/ (all tracked); git status --porcelain docs/superpowers/specs/ -> only ?? C4-smack-recovery-STAGE0-SPEC.md`
  - judgment: **designed-route_back** | confidence: verified

## Open operator blockers
- **FRAME A.1 â€” detail-page button-bar frames (THE keystone)** - Needed: a nav-panel row's DETAIL page in BOTH lock states (LOCK/UNLOCK DESTINATION) and BOTH supercruise states (ACTIVATE/DEACTIVATE SUPERCRUISE ASSIST), full button bar + order. Unblocks the entire C1 nav_target_star / nav_supercruise_* family, which C3/C4/C6/C7 all build on. No such fixture in repo (C1 design verified by enumerating tests/fixtures/navpanel/ â€” all PNGs are list/full views or icon templates).
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-CONSOLIDATED-BLOCKERS.md:13 :: Read CONSOLIDATED-BLOCKERS.md 13-17; corroborated C1-DESIGN.md:8`
  - judgment: **blocked-on-operator** | confidence: verified
- **FRAME A.2 â€” contacts-tab station-distance frame at approach range** - Needed: the station distance at approach range to learn whether it reads km/Mm and on which tab. The READ layer parses only Ls/Ly today (navpanel_reader._DISTANCE_RE matches LS|LY only), so the <7.5 km docking gate (C7) needs a new km-parser calibrated against a real frame. None exists â†’ the entire C7 <7.5km proximity leg may be unbuildable until pinned.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-CONSOLIDATED-BLOCKERS.md:18 :: Read CONSOLIDATED-BLOCKERS.md 18-20; corroborated C7 ledger dissent`
  - judgment: **blocked-on-operator** | confidence: verified
- **FRAME A.3 â€” smack no-vector deliberate-drop frame (+ later purple planet-smack)** - Needed: the negative case (deliberate drop, NO vector) and later a purple planet-smack frame, to calibrate ed_vision/escape_vector.py (confirmed STILL a NONE-returning stub, STUB-001). The C6 column-0 icon classifier MIGHT be buildable on the two existing frames (lhs2509_unexplored + shinrarta_populated) â€” the author will attempt it, new frame only if validation fails.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-CONSOLIDATED-BLOCKERS.md:22 :: Read CONSOLIDATED-BLOCKERS.md 22-25; Read escape_vector.py:62-92 (returns NONE)`
  - judgment: **blocked-on-operator** | confidence: verified
- **GAME-TRUTH B.4 â€” Status.Destination station schema live test (C2 D1)** - Needed: undock -> plot a route to a STATION -> read Status.json -> RESOLVED 2026-06-21 (D1-DESTINATION-DISCRIMINATOR-FINDING.md): station iff Body!=0 AND Name!=currentSystemName (a locked STAR is Body!=0 too). The whole system-vs-station branch (Arrival->Docking vs Traversal) hinges on this â€” the one STILL-BLOCKED C2 item after the round-2 pins. dest_is_station/exploration_active fail closed until it lands.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-CONSOLIDATED-BLOCKERS.md:39 :: Read CONSOLIDATED-BLOCKERS.md 39-41; corroborated C2 brief:17`
  - judgment: **blocked-on-operator** | confidence: verified
- **GAME-TRUTH B.5 â€” does SC-assist on an UNEXPLORED row DROP or ORBIT? (C6 B3/B4)** - Needed: if SC-assist on an unexplored row drops the ship into normal space (vs orbiting), the explore loop needs a re-engage branch or it strands. Memory says SC-assist orbits bodies in SC and drops only at stations/POI, but unexplored bodies are the untested case. C6's visit gate currently has NO drop/SupercruiseExit recovery branch â€” if it drops, all three C6 candidates strand the ship.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-CONSOLIDATED-BLOCKERS.md:43 :: Read CONSOLIDATED-BLOCKERS.md 43-45; corroborated C6 ledger dissent`
  - judgment: **blocked-on-operator** | confidence: verified
- **C4 smack-routine DEVIATION â€” operator must reconcile verbal routine vs authored spec vs live memory** - The single hardest blocker. The operator's fresh verbal smack routine (target NOTHING + escape-vector fine-align) conflicts with his own authored master-spec (nav_target_star + pitch_compass) AND with live memory (compass CV blind to the real escape vector; compass needs a locked target). Must be reconciled by the operator before C4 can re-fire. Step-6 verb is settled (engage_supercruise/Key_J) but moot until the routine is chosen.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\council-briefs\C4-smack-recovery.md:3 :: Read C4 brief 3-10 (DEVIATION-BLOCKED banner) + CONSOLIDATED-BLOCKERS.md 51-57`
  - judgment: **blocked-on-operator** | confidence: verified
- **Intent decisions #6-#13 â€” LOCKED 2026-06-18 (except C4), not yet reflected in code** - Consolidated Section C (36-50) marks eight intent decisions LOCKED: #9 exploration flag->reuse body_tour_enabled; #6 smack step-6->engage_supercruise [moot pending C4]; #8 planet-smack->same as star-smack parameterized, compass usable throughout; #11 docking->OCR-gated; #12 NFZ-entry != docking-ready; #13 station-name->Status.Destination.Name (logging-only); #7 nav_supercruise_star->assumes already in SC. Transition mechanism #10 resolved to Python orchestrator + mandatory abort-recheck (5dfc476). These locks pin HOW the still-unbuilt designs should be built; what remains blocking is the frames (A) + game-truth tests (B) + the C4 deviation.
  - proof: `<repo-root>\ED-AFK\docs\superpowers\specs\2026-06-17-flow-redesign-CONSOLIDATED-BLOCKERS.md:36 :: Read CONSOLIDATED-BLOCKERS.md 36-57; git log -> 5dfc476, fbd17ae`
  - judgment: **blocked-on-operator** | confidence: verified

## Unresolved / disputed
- Worktree branch enumeration: the slice's per-group range lists sum to ~89-92 seats, but the authoritative `git branch | grep worktree | wc -l` returns 88. I could not perfectly reconcile every named seat to a group because the slice used compressed range notation (e.g. wf_449df9ea-091-{2..10}) that overlaps the corrected additions; the load-bearing fact is settled (all 88 are IS_ANCESTOR/cruft-delete) but the exact per-group membership has a +/-1 ambiguity in three groups. Not material to the delete decision.
- C2 reference discriminators: I confirmed dest_is_system/dest_is_station/exploration_active are absent from predicates.py (grep 0) and that the slice says they were '29-test-GREEN inside the test file', but I did NOT re-run that 29-test suite (it lived in a now-cleaned council worktree). The BUILT-vs-DESIGNED verdict (designed-only) does not depend on it, but the claim 'the reference impl is green' is inherited from the slice, not re-verified here.

## Ledger entries
- DEFECT (major, judgment lens, APPLIED): slice stubs-unwired-dead classified ed_trading as 'dead-delete / never imported by any live production path' â€” factually wrong. ed_trading is auto-activated every run via [project.entry-points."ed_autojump.plugins"] trading=ed_trading:activate (pyproject.toml:30) loaded by cli.py:493-495. Reclassified to empty-scaffold/live-wired-no-op; do NOT delete (would drop the reserved plugin slot). ed_combat (no entry-point) remains correctly inert.
- DEFECT (minor, judgment lens, APPLIED): branches-and-worktrees under-counted worktree branches as 85 and the 1ad0746 group as 44. Authoritative count is 88 total / 47 in the 1ad0746 group; the three missed seats (wf_bc37f748-3ce-9, -10, wf_e93e77aa-776-9) were re-verified at tip 1ad0746 / IS_ANCESTOR. Cruft-delete verdict unchanged for all.
- DEFECT (minor, both lenses, APPLIED): UNWIRED-001 cited a proof grepping both dispatcher.py and cli.py; the cli.py arm returns zero output (it confirms ABSENCE of a factory, not a hit). Proof corrected to dispatcher.py:200 as the positive assignment, with cli.py-empty as corroborating absence.
- DEFECT (minor, proof-resolution lens, APPLIED): STUB-008's proof arm 'TODO.*operator.*kind' matches zero lines (actual text 'TODO(operator): how to read body KIND per nav-panel row?' has lowercase 'kind' after a colon). Re-pointed the proof to the Q6 arm (lines 37/45/49/54), which carries the evidence. Stub substance unaffected.
- SHARPENING (arbiter, re WT-002): the new import-resolution test FAILS on exactly 74 violations (re-run confirmed). Its one flight-code violation, steps.py:1471 'from ed_vision.hud_sc_indicators import detect_orbiting', targets a module that genuinely does not exist (ModuleNotFoundError) BUT is defensively dead at runtime â€” step_confirm_orbiting returns at the hud_grabber-is-None guard (line 1464, always None live) before reaching the import, which is additionally wrapped in try/except. So it is documented debt (an unbuilt D3 dependency awaiting a separate ed-vision matcher council), not a live crash. Commit the test as a FAILING gate with that note.
- UNRESOLVED DISSENT (arbiter): exact per-group worktree branch membership has a +/-1 ambiguity in three groups because the slice used compressed range notation that overlaps the corrected additions; total (88) and the delete verdict (all IS_ANCESTOR/cruft) are settled, the per-group breakdown is not perfectly reconciled.
- UNRESOLVED (arbiter, inherited not re-verified): the C2 reference discriminators are reported '29-test-GREEN' by the slice but that suite lived in a now-cleaned worktree and was not re-run here; the designed-only verdict is independent of it (the predicates are absent from predicates.py, grep 0), but the 'green' claim is inherited.
